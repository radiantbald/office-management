/**
 * Application entry point.
 *
 * Extracted modules live in separate files; the bulk of the app
 * still resides in app.js and will be gradually migrated.
 */

// Styles — processed by Vite (CSS bundling, minification, hashing)
import "../styles.css";

// Side-effect import — initializes the entire application
import "./app.js";
